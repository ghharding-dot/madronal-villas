const button=document.querySelector('.menu');const nav=document.querySelector('.header nav');if(button&&nav){button.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')))}const form=document.querySelector('#enquiry-form');if(form){form.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form);const subject=encodeURIComponent(`Madroñal Villas enquiry — ${data.get('villa')||'General'}`);const body=encodeURIComponent([`Name: ${data.get('name')||''}`,`Email: ${data.get('email')||''}`,`Telephone / WhatsApp: ${data.get('phone')||''}`,`Preferred villa: ${data.get('villa')||''}`,`Arrival: ${data.get('arrival')||''}`,`Departure: ${data.get('departure')||''}`,'',`${data.get('message')||''}`].join('\n'));window.location.href=`mailto:info@madronalvillas.com?subject=${subject}&body=${body}`})}