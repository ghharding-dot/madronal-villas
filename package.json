{
  "name": "madronal-villas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html villa-candela.html villa-lampara.html dist/ && cp -R assets dist/assets"
  }
}
